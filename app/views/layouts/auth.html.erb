<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title>MyApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="m-0 h-full">
  <div class="flex items-stretch min-h-screen bg-gray-50">
    <!-- Image Container -->
    <div class="flex-1 h-full w-full max-w-4xl mx-auto bg-white rounded-lg shadow-xl">
      <div class="flex flex-col md:flex-row h-full">
        <!-- Image Section -->
        <div class="h-32 md:h-auto md:w-1/2 flex-none">
          <%= image_tag "auth.png", class: "object-cover w-full h-full" %>
        </div>

        <!-- Content Section -->
        <div class="flex items-stretch p-6 sm:p-12 md:w-1/2">
          <div class="w-full">
            <div class="sm:mx-auto sm:w-full sm:max-w-md">
              <%= yield(:auth_header) if content_for?(:auth_header) %>
            </div>
            <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
              <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
                <p class="notice"><%= notice %></p>
                <p class="alert"><%= alert %></p>
                <%= yield %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>


</html>
